export interface registrationResponseDTO {
    did: string;
    ebsi_access_token: string;
    apptoken: string;
}

export interface loginResponseDTO {
    did: string;
    ebsi_access_token: string;
    apptoken: string;
}